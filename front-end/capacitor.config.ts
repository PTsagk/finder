import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'diadiktiakos-programatismos',
  webDir: 'www'
};

export default config;
